<h1 align="center"><%=@user.username %>'s page</h1>
 <br/>
  <br/>
  <div class="row">
    <div class="col-md-4 col-md-offset-4 center">
      <%= gravatar_for @user, size: 150 %>
      
    </div>
  </div>
  
  
<h4 align="center"><%=@user.username %>'s loads</h4>  

<%= render 'loads/load', obj: @user.loads %>
