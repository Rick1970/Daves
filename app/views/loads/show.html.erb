<p id="notice"><%= notice %></p>

  <ul class="listing">
        <div class="row center">
          <div class="col-md-4 col-md-offset-4">
             <li><%= link_to gravatar_for(@load.user), user_path(@load.user) %></li>
             <li class="article-title"><%= link_to @load.user.username, user_path(@load.user) %></li>
              <li><small><%= pluralize(@load.user.loads.count, "load") if @load.user.loads %></small></li>
          </div>
        </div>
    </ul>    
<p>
  <strong>Number:</strong>
  <%= @load.Number %>
</p>

<p>
  <strong>Customer:</strong>
  <%= @load.Customer %>
</p>

<p>
  <strong>From:</strong>
  <%= @load.From %>
</p>

<p>
  <strong>To:</strong>
  <%= @load.To %>
</p>


<%= link_to 'Edit', edit_load_path(@load) %> |
<%= link_to 'Back', loads_path %>
